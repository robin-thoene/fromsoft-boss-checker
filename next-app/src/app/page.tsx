import { ReactElement } from 'react';

/**
 * Default root page to render at "/", not relevant because of permanent redirect.
 * @returns {ReactElement} Component to render at the application root.
 */
export default function Page(): ReactElement {
    return <></>;
}
